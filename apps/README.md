# Applications

- `swaputer-web` is the protocol explorer. Studio is an independent online
  frontend; Mint, Market, and Bridge live in the standalone `computer` frontend.
- `swaputer-inspector` is a standalone inspection utility.

The product navigation links to the standalone Computer and Studio frontends.
Legacy application routes remain only as compatibility redirects.
