# Web application

`swaputer-web` is the read-only Swaputer protocol explorer. It contains no
wallet connection, embedded protocol applications, or compatibility redirects
for applications that have moved to standalone products.
